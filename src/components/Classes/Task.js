class Task {
    constructor(tags, points, dueDate, checklist, notes, name, assignedProject) {
        this.tags = tags;
        this.points = points;
        this.dueDate = dueDate;
        this.checklist = checklist;
        this.notes = notes;
        this.name = name;
        this.assignedProject = assignedProject;
    }
}