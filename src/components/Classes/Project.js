class Project {
  constructor(tasks, users, customer, budget, startDate, desiredEndDate, comments, skills) {
    this.tasks = tasks;
    this.users = users;
    this.customer = customer;
    this.budget = budget;
    this.startDate = startDate;
    this.desiredEndDate = desiredEndDate;
    this.comments = comments;
    this.skills = skills;
  }
}